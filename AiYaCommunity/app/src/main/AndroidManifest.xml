<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.nanchen.newsdemo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".home.music.MusicService">
        </service>

        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".mine.RegistActivity">
        </activity>
        <activity android:name=".mine.LoginActivity">
        </activity>
        <activity android:name=".home.TranslateActivity">
        </activity>
        <activity android:name=".home.MusicActivity">
        </activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".GuideActivity">
        </activity>
        <activity android:name=".msg.ChatActivity">
        </activity>
    </application>

</manifest>
